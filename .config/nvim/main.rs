fn main() {
    // Print a greeting
    println!("Hello, Rust LSP!");

    // Simple variable and arithmetic
    let x = 5;
    let y = 10;
    let sum = x + y;

    println!("Sum of {} and {} is {}", x, y, sum);

    println!("hello world");
    if x >= y {
        println!("x is larger than y");
    }
}
